"use strict";

function get100Numbers() {
  let arr = 0;
  // arr.push(100);
  // let newArr = arr + 1;
  for (let i = 0; i < 100; i++) {
    arr = arr + 1;
    console.log(arr);
    console.log(arr.push);

    // const newArr = arr + arr.push();
  }
}
get100Numbers();
